//  BEHIND THE SCENES

/*

const pathName = window.location.pathname; ex: /play, /fitness

if ((pathName = "fitness")) {
  return <FitneessPage />;
} else if ((pathname = "care")) {
  return <CarePage />;
} else if (pathname = "sport") {
  reutrn <SportsPage />
} 
else {
  return null
}

// swtich we have to destory the HEAP. 

*/
